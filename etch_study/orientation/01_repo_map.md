# Repo map

**Question**

Map the repo. For each top-level directory (`src/`, `scripts/`, `datafolder/`, `external/`, `assets/`) and each file in `src/` and `scripts/`, give one sentence on its role and whether it runs at data-prep, train, eval, or inference time.

**Ask Claude Code for:** `file:line` references and tensor shapes where relevant.

---

## Answer

<!-- Claude Code fills this in. -->
