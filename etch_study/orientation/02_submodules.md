# Submodules

**Question**

What are the git submodules in `.gitmodules`, and why is `theseus` cloned and pip-installed separately rather than listed in `environment.yml`?

**Ask Claude Code for:** `file:line` references and tensor shapes where relevant.

---

## Answer

<!-- Claude Code fills this in. -->
